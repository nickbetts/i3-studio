import { desc } from "drizzle-orm";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Checkbox } from "@/components/ui/checkbox";
import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { db } from "@/db";
import { clientAccounts, users } from "@/db/schema";
import { requireAdmin } from "@/lib/auth-helpers";
import { createTeammate, updateClientTabs, updateUserAccess } from "./actions";

const tabOptions = [["dashboard", "Dashboard"], ["projects", "Projects"], ["approvals", "Approvals"], ["designs", "Designs"], ["support", "Support"]] as const;

export default async function AgencySettingsPage() {
  await requireAdmin();
  const [team, clients] = await Promise.all([db.query.users.findMany({ where: (user, { inArray }) => inArray(user.role, ["admin", "account_manager"]), orderBy: desc(users.createdAt) }), db.query.clientAccounts.findMany({ orderBy: desc(clientAccounts.name) })]);
  return <div className="space-y-6"><div><h1 className="text-2xl font-semibold">Admin settings</h1><p className="text-muted-foreground">Manage teammates, roles and portal visibility.</p></div><Card><CardHeader><CardTitle className="text-base">Add teammate</CardTitle><CardDescription>Create an active internal login.</CardDescription></CardHeader><CardContent><form action={createTeammate} className="grid gap-4 md:grid-cols-4"><Input name="name" placeholder="Name" required /><Input name="email" type="email" placeholder="Email" required /><Input name="password" type="password" minLength={8} placeholder="Temporary password" required /><Select name="role" defaultValue="account_manager"><SelectTrigger><SelectValue /></SelectTrigger><SelectContent><SelectItem value="account_manager">Account manager</SelectItem><SelectItem value="admin">Admin</SelectItem></SelectContent></Select><Button type="submit">Add teammate</Button></form></CardContent></Card><Card><CardHeader><CardTitle className="text-base">Internal users</CardTitle><CardDescription>Set a user role and the agency tabs they can access.</CardDescription></CardHeader><CardContent className="space-y-4">{team.map((member) => { const permissions = member.permissions as { tabs?: unknown } | null; const allowed = Array.isArray(permissions?.tabs) ? permissions.tabs : tabOptions.map(([value]) => value); return <form key={member.id} action={updateUserAccess} className="flex flex-wrap items-center gap-4 border-b pb-4 last:border-0"><input type="hidden" name="userId" value={member.id} /><div className="w-48"><p className="font-medium">{member.name || member.email}</p><p className="text-xs text-muted-foreground">{member.email}</p></div><Select name="role" defaultValue={member.role}><SelectTrigger className="w-40"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="account_manager">Account manager</SelectItem><SelectItem value="admin">Admin</SelectItem><SelectItem value="client">Client</SelectItem></SelectContent></Select><div className="flex flex-wrap gap-3">{tabOptions.map(([value, label]) => <label key={value} className="flex items-center gap-2 text-sm"><Checkbox name={`tab-${value}`} defaultChecked={allowed.includes(value)} value="on" />{label}</label>)}</div><Button type="submit" variant="outline">Save access</Button></form>; })}</CardContent></Card><Card><CardHeader><CardTitle className="text-base">Client portal tabs</CardTitle><CardDescription>Control which areas each client account can see.</CardDescription></CardHeader><CardContent className="space-y-4">{clients.map((client) => { const allowed = Array.isArray(client.visibleTabs) ? client.visibleTabs : tabOptions.map(([value]) => value); return <form key={client.id} action={updateClientTabs} className="flex flex-wrap items-center gap-4 border-b pb-4 last:border-0"><input type="hidden" name="clientAccountId" value={client.id} /><p className="w-40 font-medium">{client.name}</p><div className="flex flex-wrap gap-3">{tabOptions.map(([value, label]) => <label key={value} className="flex items-center gap-2 text-sm"><Checkbox name={`client-tab-${value}`} defaultChecked={allowed.includes(value)} value="on" />{label}</label>)}</div><Button type="submit" variant="outline">Save tabs</Button></form>; })}</CardContent></Card></div>;
}
